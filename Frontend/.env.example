# Base URL of the backend API (no trailing slash)
VITE_API_URL=https://localfinder-ai.onrender.com

# Local development override (create a .env or .env.local with this instead):
# VITE_API_URL=http://localhost:5000
